from django.apps import AppConfig


class StatusesConfig(AppConfig):
    """Configuration class for the statuses app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'task_manager.statuses'
